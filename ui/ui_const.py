"""
UI 统一常量池 — 色彩、尺寸、布局参数。

所有 UI 组件禁止硬编码 RGB / 尺寸数值，统一从此模块引用。
零侵入原则：此模块仅被 ui/ 目录下的文件导入，game/ 层不依赖。

渲染分层协议（Strict Layering Protocol）：
  层1  桌面背景   BG_CREAM + 网格纹理
  层2  阵营占领底色  alpha=40 圆角矩形
  层3  选中/悬浮发光  BORDER_HOVER
  层4  地块边框    width=2
  层5  地形贴图    TILE_PADDING 内缩
  层6  星星/资源图标  居中或角标
  层7  士兵棋子    最顶层
"""

import pygame

# ═══════════════════════════════════════════════════════════════
#  基础配色 (Palette)
# ═══════════════════════════════════════════════════════════════

BG_CREAM       = (248, 245, 238)       # 桌面浅米木色
ROAD_COLOR     = (180, 160, 130)       # 道路双线颜色（温暖沙色）
PANEL_BG       = (255, 253, 248, 220)  # 面板底色 (带 Alpha)

BORDER_HOVER   = (180, 180, 255)       # 地块悬浮边框（淡蓝紫）
BORDER_WHITE   = (255, 255, 255)       # 驻军白色细边框

TEXT_MUTED     = (120, 120, 120)       # 次要/辅助文字

# 区块包围方框配色（按 area_id 循环取色）
AREA_BOUNDS_COLORS = [
    (255, 245, 220), (220, 240, 255), (240, 255, 220),
    (255, 220, 240), (220, 255, 245), (245, 230, 255),
]

# ═══════════════════════════════════════════════════════════════
#  高亮与状态色
# ═══════════════════════════════════════════════════════════════

FALLBACK_GRAY  = (200, 200, 200)   # 颜色查表失败时的默认灰色
TOOLTIP_BG     = (40, 40, 50)      # Tooltip 深色背景
FACTION_BG_ALPHA  = 40                   # 阵营占领底色 Alpha（7层协议层2）

# ═══════════════════════════════════════════════════════════════
#  布局与尺寸参数
# ═══════════════════════════════════════════════════════════════

TILE_PADDING   = 4      # 地块内缩边距

# 桌面网格纹理
GRID_LINE_ALPHA    = 15     # 网格线 alpha（0-255），6% ≈ 15
GRID_TILE_SIZE     = 32     # 平铺网格单元尺寸 (px)
GRID_LINE_WIDTH    = 1      # 网格线宽度
GRID_LINE_COLOR    = (0, 0, 0, GRID_LINE_ALPHA)  # 网格线颜色

# 手牌马卡龙阵营色（玩具风柔和边框/底座）
TOY_RED_BORDER  = (255, 150, 140)   # 柔和珊瑚红
TOY_BLUE_BORDER = (140, 180, 255)   # 淡蓝

# ═══════════════════════════════════════════════════════════════
#  相机控制参数
# ═══════════════════════════════════════════════════════════════

CAMERA_MIN_ZOOM    = 0.4    # 最小缩放倍率
CAMERA_MAX_ZOOM    = 2.5    # 最大缩放倍率
CAMERA_ZOOM_FACTOR = 1.12   # 滚轮缩放因子（>1 放大，<1 缩小）
CAMERA_SMOOTH_SPEED = 8.0   # 平滑居中插值速度
CAMERA_FIT_PADDING  = 0.95  # 自适应缩放边距比例

# ═══════════════════════════════════════════════════════════════
#  圆角规范（统一全UI圆角半径）
# ═══════════════════════════════════════════════════════════════
RADIUS_SM = 8       # 小元素（标签、角标）
RADIUS_MD = 12      # 卡片、按钮
RADIUS_LG = 18      # 面板、弹窗
RADIUS_XL = 24      # 大面板

# ═══════════════════════════════════════════════════════════════
#  阴影规范
# ═══════════════════════════════════════════════════════════════
SHADOW_OFFSET = (3, 4)
SHADOW_ALPHA = 50

# ═══════════════════════════════════════════════════════════════
#  对手手牌区
# ═══════════════════════════════════════════════════════════════
OPP_CARD_W = 48
OPP_CARD_H = 66
OPP_CARD_GAP = 4
OPP_CARD_Y = 12
OPP_HAND_Y = 8

# ═══════════════════════════════════════════════════════════════
#  弃牌堆位置
# ═══════════════════════════════════════════════════════════════
DISCARD_PILE_X = 1220
DISCARD_PILE_Y = 660